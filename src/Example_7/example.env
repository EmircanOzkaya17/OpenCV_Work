
IMAGE_PATH=assets/kiwi.jpg


OUTPUT_DIR=results

# ─── Saturation Threshold ─────────────────────────────────────────────────────────
# Recommended range: 45–70
# Low → more pixels are captured, noise increases
# High → only intensely colored pixels are captured
SATURATION_THRESHOLD=55

# ─── Minimum Area Filter ────────────────────────────────────────────────────
# Recommended range: 0.006–0.012
MIN_AREA_RATIO=0.009

# ───Maximum Aspect Ratio────────────────────────────────────────────────────
# Recommended range: 1.8–2.5
MAX_ASPECT_RATIO=2.0

# ─── Convex Hull Circularity Threshold ────────────────────────────────────────────
# Recommended range: 0.35–0.55
MIN_HULL_CIRCULARITY=0.40

# ─── Ellipse Expansion Factor ─────────────────────────────────────────────
# Recommended range: 1.03–1.12
ELLIPSE_SCALE=1.06

# ─── GrabCut Iteration Count ─────────────────────────────────────────────────
# Recommended range: 3–8
GRABCUT_ITERATIONS=4

# ─── GrabCut Edge Buffer (pixel) ────────────────────────────────────────────
# Recommended range: 12–25
GRABCUT_BORDER_PX=18

# ─── Save Search Images ────────────────────────────────────────────────────
# True → intermediate step images are also saved | False → only final outputs are saved
SAVE_INTERMEDIATE=True